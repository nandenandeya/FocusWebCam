# FocusWebCam — Proyek 3 (Streamlit with Premium UI)

Proyek ini menggabungkan:
- **Logika deteksi fokus 100% identik** dengan Proyek 2 (Streamlit + MediaPipe + Logistic Regression)
- **UI/UX semirip mungkin** dengan Proyek 1 (HTML/CSS/JS localhost)

## Cara Menjalankan

1. Clone/download folder ini.
2. Install dependencies:
   ```bash
   pip install -r requirements.txt